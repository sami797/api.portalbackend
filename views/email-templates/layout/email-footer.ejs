<div class="footer">
    <% if(hideFooter !==true){ %>
        <% if(unsubscribeUrl){ %>
            <p style="margin-bottom: 0px;">To stop receiving this series of emails, please <a
                    href="<%= unsubscribeUrl %>" target="_blank" class="text-white">unsubscribe</a>.
            </p>
            <p style="margin:0px">Alternatively, <a class="text-white" href="<%= notificationPreferences %>"
                    target="_blank">manage your email preferences</a>.</p>
            <% }else{ %>
                <a class="text-white" href="<%= notificationPreferences %>" target="_blank">Manage your email
                    preferences</a>
                <% } %>
                    <% } %>
                        <p><%= companyAddress %></p>
                        <div class="social-wrap">
                            <a href="<%= facebookUrl %>" target="_blank"><img
                                    src="<%= process.env.IMAGE_CDN_URL %>public/email-templates/facebook.png" /></a>
                            <a href="<%= instagramUrl %>" target="_blank"><img
                                    src="<%= process.env.IMAGE_CDN_URL %>public/email-templates/instagram.png" /></a>
                            <a href="<%= linkedInUrl %>" target="_blank"><img
                                    src="<%= process.env.IMAGE_CDN_URL %>public/email-templates/linkedin.png" /></a>
                        </div>
</div>


</div>
</body>

<style>
    :root {
        font-size: 16px;
    }

    body {
        background-color: #f2f2f2;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        color: rgba(89, 89, 89, 1);
    }

    .logo {
        min-height: 30px;
        max-height: 50px;
        max-width: 200px;
        object-fit: contain;
    }

    .mb-0 {
        margin-bottom: 0px;
    }

    .text-white {
        color: white;
    }

    .container {
        max-width: 600px;
        margin: 0 auto;
        background-color: white;

    }

    .header {
        text-align: center;
        padding: 1rem 2rem;
        display: block;
        justify-content: center;
        border-top: 15px solid #f2f2f2;
    }

    .welcome-banner {
        background-color: rgba(19, 119, 72, 1);
        padding: 1rem;
        font-size: x-large;
        color: white;
    }

    .welcome-text {
        padding: 1rem;
        border-bottom: 5px solid #F2f2f2;
    }

    .welcome-text>.heading {
        font-size: 14px;
        color: rgba(19, 119, 72, 1);
        ;
        margin-bottom: 0.5rem;
    }

    .welcome-text>.description {
        font-size: 14px;
        line-height: 1.5;
    }

    .footer {
        padding: 1rem;
        background-color: rgba(35, 36, 42, 1);
        color: #fff;
        align-items: center;
        text-align: center;
        font-size: 0.75rem;
    }

    .social-wrap {
        display: block;
        width: 100%;
        text-align: center;
    }

    .social-wrap a {
        padding: 10px;
    }

    .social-wrap img {
        height: 20px;
        width: 20px;
        object-fit: contain;
    }

    .text-center {
        text-align: center;
    }

    .link-button {
        display: inline-block;
        justify-content: center;
        margin: auto;
        background-color: rgba(19, 119, 72, 1);
        color: white;
        text-decoration: none;
        /* width: 25%; */
        padding: 1rem;
        border-radius: 0.25rem;
        cursor: pointer;
        font-size: small;
    }

    .ml-2 {
        margin-left: 15px;
    }
</style>

</html>